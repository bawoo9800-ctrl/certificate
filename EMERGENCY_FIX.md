# 🚨 긴급 수정: 발행내역 JavaScript 오류 해결

## 문제 증상

```
history.js?v=10.13:153 Uncaught SyntaxError: Unexpected token 'catch'
발행내역을 불러올 수 없음
```

## 원인

NAS 서버에 이전 버전의 잘못된 `history.js` 파일이 캐시되어 있습니다.

---

## 🔧 해결 방법 (NAS SSH에서 실행)

### 전체 명령어 (한 번에 실행)

```bash
cd /volume1/web/certificate && \
git fetch origin main && \
git reset --hard origin/main && \
echo "✅ 최신 코드로 완전히 업데이트 완료!" && \
echo "📋 변경된 파일:" && \
git log --oneline -3
```

### 단계별 설명

#### 1단계: 프로젝트 디렉토리 이동
```bash
cd /volume1/web/certificate
```

#### 2단계: 원격 저장소 최신 상태 가져오기
```bash
git fetch origin main
```

#### 3단계: 로컬 파일을 원격 저장소와 완전히 동기화 (강제)
```bash
git reset --hard origin/main
```
⚠️ **주의:** 이 명령은 로컬의 모든 변경사항을 버리고 원격 저장소 버전으로 완전히 덮어씁니다.

#### 4단계: 업데이트 확인
```bash
git log --oneline -3
```

**예상 출력:**
```
8ef74c2 fix: history.js 버전을 10.14로 업데이트 (브라우저 캐시 우회)
76f2f27 docs: NAS 서버에 수정사항 적용 가이드 추가
f6ba9a1 docs: NAS 배포 가이드 추가 - Apache 3003 포트 설정 및 리버스 프록시 가이드
```

#### 5단계: 파일 내용 검증
```bash
# history.html의 버전 확인
grep "history.js" history.html
```

**예상 출력:**
```html
<script src="js/history.js?v=10.14"></script>
```

#### 6단계: history.js의 수정 내용 확인
```bash
sed -n '114,118p' js/history.js
```

**예상 출력:**
```javascript
            console.log('🔍 검색 결과:', filteredData.length + '건');
            // 검색 시에만 totalCount를 검색 결과 개수로 업데이트
            totalCount = filteredData.length;
        }
        
```

---

## 🌐 브라우저에서 테스트

### 방법 1: 강력 새로고침 (권장)

1. 발행내역 페이지 접속:
   ```
   http://192.168.0.109:3003/history.html
   ```

2. **강력 새로고침** (캐시 무시):
   - Windows/Linux: `Ctrl + Shift + R`
   - Mac: `Cmd + Shift + R`

3. 개발자 콘솔 확인 (`F12`):
   ```javascript
   // 콘솔에 에러가 없어야 함
   // "총 발행 건수: 325" 정상 표시
   ```

### 방법 2: 브라우저 캐시 완전 삭제

1. `F12` → `Application` (또는 `애플리케이션`) 탭
2. 좌측 메뉴 → `Storage` → `Clear site data` 클릭
3. 페이지 새로고침

### 방법 3: 시크릿/프라이빗 모드

1. 시크릿 창 열기:
   - Chrome: `Ctrl + Shift + N`
   - Firefox: `Ctrl + Shift + P`
   - Safari: `Cmd + Shift + N`

2. 새 창에서 접속:
   ```
   http://192.168.0.109:3003/history.html
   ```

---

## ✅ 정상 작동 확인

### 1. 콘솔 로그 확인

`F12` → `Console` 탭에서 다음과 같은 로그가 보여야 합니다:

```
🔍 검색 조건: {page: 1, limit: 10, sortOrder: "-created_at", searchQuery: ""}
📡 발행 내역 조회 중... api/certificates.php?page=1&limit=10&sort=-created_at
✅ 발행 내역 조회 완료: {data: Array(10), total: 325, ...}
📊 API에서 받은 데이터 길이: 10
📊 API에서 받은 총 건수: 325
📊 통계 카드 업데이트 시작
  ✅ 총 발행 건수: 325
```

### 2. 화면 표시 확인

- **총 발행 건수:** 325 ✅
- **오늘 발행:** (실제 값)
- **이번 주 발행:** (실제 값)
- **테이블에 10개 항목 표시**
- **페이지네이션: 1, 2, 3, ... 33** (325 ÷ 10 = 33페이지)

### 3. 검색 기능 테스트

1. 검색창에 업체명 입력 (예: "정일")
2. 검색 버튼 클릭
3. 총 발행 건수가 검색 결과 수로 변경되는지 확인
4. 새로고침 버튼 클릭
5. 총 발행 건수가 다시 325로 복귀하는지 확인

---

## 🐛 여전히 문제가 발생하면

### 문제 1: git reset 실패 - 권한 오류

```bash
# 디렉토리 소유권 확인
ls -la /volume1/web/certificate

# 소유권 변경 (필요시)
sudo chown -R admin:users /volume1/web/certificate

# 다시 시도
git reset --hard origin/main
```

### 문제 2: 여전히 v=10.13으로 로드됨

**원인:** Apache 또는 브라우저의 심각한 캐시 문제

**해결방법:**

```bash
# Apache 재시작
sudo /usr/local/bin/apachectl restart

# 또는 DSM에서 Apache 패키지 재시작
# 패키지 센터 → Apache HTTP Server 2.4 → 중지 → 시작
```

### 문제 3: 콘솔에 404 에러

```
GET http://192.168.0.109:3003/js/history.js?v=10.14 404 (Not Found)
```

**해결방법:**

```bash
# 파일이 실제로 존재하는지 확인
ls -la /volume1/web/certificate/js/history.js

# 파일 권한 확인
chmod 644 /volume1/web/certificate/js/history.js

# Apache가 읽을 수 있는지 확인
sudo -u http cat /volume1/web/certificate/js/history.js | head -5
```

### 문제 4: API 연결 실패

```
Failed to fetch
```

**해결방법:**

```bash
# API 테스트
curl http://192.168.0.109:3003/api/certificates.php?page=1&limit=10

# PHP-FPM 상태 확인
ps aux | grep php-fpm

# Apache 로그 확인
tail -f /var/packages/Apache2.4/target/var/log/error_log
```

---

## 📊 변경 이력

| 커밋 | 날짜 | 변경 내용 |
|------|------|-----------|
| `8ef74c2` | 2026-02-07 | history.html 버전을 v=10.14로 업데이트 |
| `4051d24` | 2026-02-07 | history.js 총 발행 건수 버그 수정 |
| `76f2f27` | 2026-02-07 | 적용 가이드 문서 추가 |

---

## 🔗 참고 링크

- **GitHub 저장소:** https://github.com/bawoo9800-ctrl/certificate
- **최신 커밋:** https://github.com/bawoo9800-ctrl/certificate/commit/8ef74c2
- **수정된 파일:**
  - https://github.com/bawoo9800-ctrl/certificate/blob/main/certificate/js/history.js
  - https://github.com/bawoo9800-ctrl/certificate/blob/main/certificate/history.html

---

## 💡 빠른 실행 명령어 (복사해서 붙여넣기)

```bash
cd /volume1/web/certificate && git fetch origin main && git reset --hard origin/main && echo "✅ 업데이트 완료! 브라우저에서 Ctrl+Shift+R 하세요!"
```

---

**작성일:** 2026-02-07  
**최종 업데이트:** 커밋 `8ef74c2`  
**작성자:** Claude AI Assistant

## 📞 확인 후 결과 보고

업데이트 후 다음 정보를 확인해 주세요:

1. ✅ `git log --oneline -1` 출력 (최신 커밋 확인)
2. ✅ 브라우저 콘솔 로그 (에러 없음)
3. ✅ 총 발행 건수: 325 표시
4. ✅ 페이지네이션 정상 작동
